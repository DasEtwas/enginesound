pub const DC_OFFSET_LP_FREQ: f32 = 4.0; // the frequency of the low pass filter which is subtracted from all samples to reduce dc offset and thus clipping

// these two are only used for reserving Ids for the sliders in the gui
pub const MAX_CYLINDERS: usize = 16;
pub const MUFFLER_ELEMENT_COUNT: usize = 4;
